import { ProgramsWorkflow } from '@/features/programs/components/programs-workflow';

export default function ProgramsPage() {
  return (
    <main className="min-h-screen bg-slate-50 dark:bg-slate-900 transition-colors duration-200">
      <ProgramsWorkflow />
    </main>
  );
}

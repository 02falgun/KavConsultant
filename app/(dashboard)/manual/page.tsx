import { ManualWorkflow } from '@/features/manual/components/manual-workflow';

export default function ManualPage() {
  return (
    <main className="min-h-screen bg-slate-50 dark:bg-slate-900 transition-colors duration-200">
      <ManualWorkflow />
    </main>
  );
}

import { GeneralSettingsWorkflow } from '@/features/settings/components/general-settings-workflow';

export default function GeneralSettingsPage() {
  return (
    <main className="min-h-screen bg-slate-50 dark:bg-slate-900 transition-colors duration-200">
      <GeneralSettingsWorkflow />
    </main>
  );
}
